<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.family.calendar.mapper.VisitMapper">

    <!-- 📌 모든 방문 목록 조회 (특정 사용자 기준) -->
    <select id="getVisitListByUser" parameterType="int" resultType="com.family.calendar.dtos.VisitDto">
        SELECT 
            v.visit_id,
            v.member_id,
            f.user_id,                                   -- ✅ user_id는 family_member에서 가져옴
            v.hospital_name,
            v.department,
            v.visit_date,
            v.cost,
            v.diagnosis,
            f.name AS member_name,
            f.relation AS member_relation
        FROM visit v
        JOIN family_member f ON v.member_id = f.member_id
        WHERE f.user_id = #{userId}
        ORDER BY v.visit_date DESC
    </select>


    <!-- 📌 특정 날짜별 방문 수 조회 (달력용) -->
    <select id="getVisitCountByDateAndUser" parameterType="map" resultType="int">
        SELECT COUNT(*) 
        FROM visit v
        JOIN family_member f ON v.member_id = f.member_id
        WHERE f.user_id = #{userId}
          AND DATE_FORMAT(v.visit_date, '%Y%m%d') = #{yyyyMMdd}
    </select>


    <!-- 📌 방문 상세 조회 -->
    <select id="getVisitDetail" parameterType="int" resultType="com.family.calendar.dtos.VisitDto">
        SELECT 
            v.visit_id,
            v.member_id,
            f.user_id,
            v.hospital_name,
            v.department,
            v.visit_date,
            v.cost,
            v.diagnosis,
            f.name AS member_name,
            f.relation AS member_relation
        FROM visit v
        JOIN family_member f ON v.member_id = f.member_id
        WHERE v.visit_id = #{visit_id}
    </select>


    <!-- 📌 새 방문 등록 -->
    <insert id="insertVisit" parameterType="com.family.calendar.dtos.VisitDto">
    INSERT INTO visit (
        member_id, hospital_name, department, visit_date, cost, diagnosis
    ) VALUES (
        #{member_id}, #{hospital_name}, #{department}, #{visit_date}, #{cost}, #{diagnosis}
    )
</insert>



    <!-- 📌 방문 수정 -->
    <update id="updateVisit" parameterType="com.family.calendar.dtos.VisitDto">
        UPDATE visit
        SET hospital_name = #{hospitalName},
            department = #{department},
            visit_date = #{visitDate},
            cost = #{cost},
            diagnosis = #{diagnosis}
        WHERE visit_id = #{visitId}
    </update>


    <!-- 📌 방문 삭제 -->
    <delete id="deleteVisit" parameterType="int">
        DELETE FROM visit WHERE visit_id = #{visit_id}
    </delete>

</mapper>